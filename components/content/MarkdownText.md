# What is this?

Blank doc used as a placeholder / reminder that we're going to override the MarkdownText component used by assistant-ui.

## Why are we overriding it?

We're overriding it because we need to control the styling and behavior of the markdown text.

## What are we doing?

We're going to replace all the components inside this component with our own to control styling and behavior. We're using the `chat` markdown components inside our MarkdownComponents.tsx file.